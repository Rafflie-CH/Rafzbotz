export const metadata = {
  title: "RafzBot Dashboard",
  description: "WA Bot Control Panel"
}

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  )
}
